import {signUp, signIn} from './user.controller';
import {createTask, updateTask, deleteTask, getAll, getTask} from './tasks.controller'

export default {
    signUp,
    signIn,
    createTask, 
    updateTask, 
    deleteTask, 
    getAll, 
    getTask
}